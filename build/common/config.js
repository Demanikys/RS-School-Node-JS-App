import * as SETTINGS from '../env';
const { PORT, NODE_ENV, MONGO_CONNECTION_STRING, JWT_SECRET_KEY, } = SETTINGS;
const AUTH_MODE = SETTINGS.AUTH_MODE === 'true';
export { PORT, NODE_ENV, MONGO_CONNECTION_STRING, JWT_SECRET_KEY, AUTH_MODE, };
