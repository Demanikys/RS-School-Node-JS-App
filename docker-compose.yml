version: '3'
services: 
    node: 
        build: .
        ports: 
            - ${PORT}:${PORT}
        depends_on: 
            - postgres
        environment: 
            - PORT=4000
    postgres:
        build: ./database